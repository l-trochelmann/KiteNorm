
deterministic: False
seed: 7

cuda_matmul_allow_tf32: True
cudnn_allow_tf32: True

trainset_path: "/fast/ltrochelmann/data/lm/slim_pajama/new_sp_15B_tokens/train"
vocab_size: 50280  # taking 50280 from the other slim_pajama configs
seq_len: 2048
sampler: 'random'
sampler_seed: 7
num_workers: 4

eval: True
validset_path: "/fast/ltrochelmann/data/lm/slim_pajama/new_valid/validation"
eval_every_steps: 32

model: 'transformer'
d_model: 512
expand: '8/3'
n_layers: 6
n_heads: 8
mlp_class: 'glu'
tie_embeddings: False
torch_compile: True

weight_init: 'gpt2_res-scale'  # choose from 'gpt2_res-scale', 'gpt2_no-scale', 'xavier_res-scale', 'xavier_no-scale', 't-fixup', (ablations: 'gpt2_glu-scale', 'gpt2_attn_scale')

ln_config: 'pre-norm'  # choose from 'no-norm', 'pre-norm', 'post-norm', 'mix-norm', (ablations: 'pre-attn-norm', 'pre-glu-norm', 'post-attn-norm', 'post-glu-norm', 'pre-LN-drop', 'post-LN-drop')
ln_style: 'RMSNorm'  # choose from 'LayerNorm', 'LayerNorm_Simple', 'RMSNorm', 'OnlyAffine', 'DetachNorm'
attn_style: 'Default'  # choose from 'Default', 'QKNorm'
drop_which_ln: 0  # Which ln to drop if using 'pre-LN-drop', choose from -1, 0, ...,  n_layers-1 (-1: out_norm)
ln_use_shift: True  # whether or not to use the bias parameter if applicable
mixLN_ratio: 0.25  # post-LN ratio when using mix-LN
qknorm_L97: 2048  # 97.5 percentile sequence length for QKNorm temperature init, or simply seq_len if fixed.

# note: step budget=token_budget/(seq_len * micro_batch_size * grad_accumulation_steps)
steps_budget: 5340  # choose 70M*20=1.4B tokens by chinchilla scaling law -> Obtain step size as 1.4B/(2048*32*4) = 5340.576...

# note 1: this is micro batch size if grad_accumulation_steps>1
# note 2: with ddp, effective batch size = batch_size * grad_accumulation_steps * ddp_world_size
micro_batch_size: 32  # 64 is the GPU limit at 6 layers and 1024 seq_len! -> GPU limit at 6L-2048 is 32
grad_accumulation_steps: 4  # target bs is 128, so we get 128/32 = 4 | Note that this was originally 8, so it was configured for much higher batch size: 64*8=512

# note: choose between {float32, float16, bfloat16}
# note: float16 data type will automatically use a GradScaler
dtype: 'bfloat16'

optim: 'adamw'
fused_optim: True
# high spectrum grid: [1.e-3, 2.e-3, 5.e-3, 1.e-2, 2.e-2, 5.e-2]
# low spectrum grid: [1.e-5, 2.e-5, 5.e-5, 1.e-4, 2.e-4, 5.e-4]
# full spectrum grid: [1.e-4, 2.e-4, 5.e-4, 1.e-3, 2.e-3, 5.e-3, 1.e-2, 2.e-2, 5.e-2]
lr: [1.e-3, 2.e-3, 5.e-3, 1.e-2, 2.e-2, 5.e-2]
weight_decay: 0.1
beta1: 0.95  # to tune, do tied grid search on 0.9, 0.95, 0.975
beta2: 0.95
grad_clip: 1.0
eps: 1.e-8

scheduler: 'wsd'
warmup_steps: 320
cooldown_steps: 0.15
lr_start: 1.e-10
lr_end: 0.0
lr_end_pct: null

log_every_steps: 16
print_progress: false

use_wandb: True
wandb_watch: False
wandb_project: 'LN-variants'
wandb_dir: '/home/ltrochelmann/LN-variants/logs/wandb'
wandb_run_name: 'tr_70M_sp_'
exp_name: 'tr_70M_sp_'
check_existing_wandb_run: False
track_model_update: True
track_grad_norm: True
track_param_update: True
track_softmax: True
track_ln_weights: True

resume: False
resume_step: null
resume_exp_name: null

out_dir: '/fast/ltrochelmann/exp/main'
save_last_checkpoint: True
save_intermediate_checkpoints: False
save_every_steps: null

save_optim: False
save_scheduler: False
save_scaler: False

over_write: False